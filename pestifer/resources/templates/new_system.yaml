# Generic system-build template used by the `pestifer new-system` subcommand.
#
# The subcommand fills in the title and the fetch sourceID, keeps only the fetch
# and psfgen tasks for a "minimal" build, and (for a "full" build) sets the
# terminate/package basenames.  Keep this template free of any structure-specific
# directives (residue/atom aliases, validate tests, solvent choices, etc.) so that
# generated configs do not inherit settings from an unrelated system.
title: New pestifer system
tasks:
- fetch:
    sourceID: XXXX
    source_format: pdb
- psfgen:
    source:
      biological_assembly: 1
- md:
    ensemble: minimize
- solvate:
- md:
    ensemble: minimize
- md:
    ensemble: NVT
    nsteps: 400
# Self-terminating NPT box-density equilibration: replaces the old ladder of
# progressively longer NPT runs.  Runs stability-bounded short restarts and stops
# when the density has converged (or max_steps is hit), writing its own convergence
# report + density-vs-time plot.  Tune drift_tol/max_steps in the YAML if needed.
- density_equilibrate:
- terminate:
    basename: my_system
    package:
      basename: prod_system
      namd:
        ensemble: NPT
