"""
Schema package for Pestifer.

Contains ``base.yaml``, the ycleptic-format specification file that defines
the structure and defaults for Pestifer's user configuration.  This is
package metadata (a schema), not a runtime resource, so it lives here rather
than under ``pestifer.resources``.
"""
